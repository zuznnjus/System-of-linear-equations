#include "TridiagonalMatrix.h"

bool TridiagonalMatrix::checkMatrix()
{
	return false;
}

long double* TridiagonalMatrix::solveTriMat(int n, long double* A, long double* B, long double* C, long double* D)
{
	int st;
	if (n < 1) st = 1;
	//else if () st = ;
	else st = 0;

	if (st == 0) {

	}
	else return 0; //komunikat
}
